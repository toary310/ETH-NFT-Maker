{
  "name": "eth-nft-maker",
  "version": "0.1.0",
  "description": "Upload your own image into an NFT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "contract": "yarn workspace contract",
    "client": "yarn workspace client",
    "test": "yarn workspace contract test",
    "dev": "yarn client start",
    "build": "yarn client build",
    "vercel-build": "cd packages/client && yarn install && yarn build"
  }
}
