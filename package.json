{
  "name": "eth-nft-maker",
  "version": "0.1.0",
  "description": "Upload your own image into an NFT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "contract": "yarn workspace contract",
    "client": "yarn workspace client",
    "test": "yarn workspace contract test",
    "verify": "npx hardhat run scripts/verify.js --network sepolia",
    "verify:sepolia": "npx hardhat run scripts/verify-sepolia.js --network sepolia",
    "verify:manual": "npx hardhat verify --network sepolia"
  }
}
